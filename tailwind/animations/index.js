const win = require('./win');
const reveal = require('./reveal');
const popIn = require('./popIn');
const error = require('./error');

module.exports = {
  error,
  popIn,
  reveal,
  win,
};
